# Technical Guide

This paragraph contains **bold**, *italic*, ~~removed text~~, and [a safe link](https://example.com).

## Lists

1. First ordered item
2. Second ordered item
   - Nested unordered item
     - Deeply nested item

> A useful technical note with `inline code`.

| Variable | Default | Purpose |
| --- | ---: | --- |
| `PORT` | 3000 | Local server port |
| `MODE` | production | Runtime mode |

```javascript
const ready = true
console.log({ ready })
```
